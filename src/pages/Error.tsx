const Error401 = ({ message = "There was an error loading this page" }) => (
  <div>{message}</div>
);

const Error402 = ({ message = "There was an error loading this page" }) => (
  <div>{message}</div>
);

const Error404 = ({ message = "There was an error loading this page" }) => (
  <div>{message}</div>
);

export { Error401, Error402, Error404 };
